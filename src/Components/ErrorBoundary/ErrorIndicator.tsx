import React from "react";
import { ErrorMessage } from "./styled";

const ErrorIndicator = () => {
  return (
    <div>
      <ErrorMessage>Ups something wrong....</ErrorMessage>;
    </div>
  );
};

export default ErrorIndicator;
